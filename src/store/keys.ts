/**
 * * Atoms key and selectors key must be unique so we place it all here to get rid of duplication
 */

// ? app
export const APP_MESSAGE = 'appMessage'
export const SEARCH_RESULT = 'searchResult'

// ? auth
export const AUTH_USER = 'authUser'

// ? follow
export const FOLLOW_STATE_FAMILY = 'followStateFamily'
export const FOLLOWING_IDS = 'followingIds'
